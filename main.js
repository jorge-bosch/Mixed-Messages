// 10 words for each random array below.
const noun = [];
const adj = [];
const verb = [];
const adverb = [];


function randomStory() {
    
}

//Console log message to be inserted in function to randomize words.
console.log(`Today I went to the zoo. I saw a(n) ${adj} ${noun} jumping up and down in its tree. He ${verb} ${adverb} through the large tunnel that led to its ${adj} ${noun}. I got some peanuts and passed them through the cage to a gigantic gray ${noun} towering above my head. Feeding that animal made me hungry. I went to get a ${adj} scoop of ice cream. It filled my stomach. Afterwards I had to ${verb} ${adverb} to catch our bus. When I got home I ${verb} my mom for a ${adj} day at the zoo. `);
